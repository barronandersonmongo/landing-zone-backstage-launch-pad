apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: mongodb-cluster-config
  title: "Generate MongoDB Cluster YAML"
  description: "Asks user for cluster details and publishes a YAML configuration file to a target GitHub repository."
spec:
  owner: gary@barry.mdbpsce.net
  type: configuration
  parameters:
    - title: Cluster Configuration
      required:
        - clusterName
        - clusterSize
        - comments
      properties:
        clusterName:
          type: string
          title: Cluster Name
        clusterSize:
          type: string
          title: Cluster Size
          enum:
            - Small
            - Medium
            - Large
        comments:
          title: Request Comments
          type: string
        junk:
          title: Junkj
          type: string
  steps:
    - id: create-file
      name: Create Cluster Config File
      action: roadiehq:utils:fs:write
      input:
        path: ./new_cluster_requests/${{ parameters.clusterName }}.yaml
        content: |
          clusterName: ${{ parameters.clusterName }}
          clusterSize: ${{ parameters.clusterSize }}

    - id: commit-changes
      name: Commit Changes
      action: github:repo:push
      input:
        repoUrl: "github.com?owner=barronandersonmongo&repo=tfe-test"
        branchName: "feature/new-cluster-${{ parameters.clusterName }}"
        sourcePath: ./new_cluster_requests/
        gitCommitMessage: "Add cluster configuration for ${{ parameters.clusterName }}"

    - id: publish-to-github
      name: Create Pull Request
      action: publish:github:pull-request
      input:
        repoUrl: "github.com?owner=barronandersonmongo&repo=tfe-test"
        defaultBranch: main  # Target branch
        branchName: "feature/new-cluster-${{ parameters.clusterName }}"  # Source branch
        title: "New Cluster Request for ${{ parameters.clusterName }}"
        description: "Cluster Size: ${{ parameters.clusterSize }}\n\nComments: ${{ parameters.comments }}"

